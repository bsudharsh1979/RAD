# Deployment

Local: `./scripts/dev.sh` or `docker compose up`.

Vercel: `apps/web` with rewrite to API.

Modal: `deploy/modal/modal_app.py` + `deploy/modal/modal_web.py`. See `docs/LAUNCH.md`.

NVIDIA: `deploy/nvidia`.
