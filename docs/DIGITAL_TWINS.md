# Digital twins

All scenarios in `TWIN_CATALOG`. Educational, not predictive of a specific GPU.

Boundary tests forbid NaN/Inf/negative latency.

KEDA/Dynamo/Grove twins are explicitly **out of course** if requested.
