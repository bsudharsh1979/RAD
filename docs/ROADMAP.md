# Roadmap

- Vendor a real Kit scene for aggregated encoder-decoder vs agent loops.
- Optional local sentence-transformer embeddings.
- Langfuse exporter when keys exist.
- Sandboxed notebook execution (explicit future; still off by default).
- Additional courses packed through the same ingestion engine.
