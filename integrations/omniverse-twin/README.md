# Omniverse twin (stub)

No Kit application shipped with `bsudharsh1979/RAD`.

This folder is the **integration contract** for a future NVIDIA Omniverse / OpenUSD scene driven by the same `TwinState` JSON as the web twins.

Do not treat files here as a working 3D product.

See `docs/omniverse-repo-analysis.md` and `docs/nvidia-omniverse-deployment.md`.
