[project]
name = "twin-engine"
version = "0.2.0"
description = "Pure-Python LLM Twin Academy simulation math (no I/O)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["twin_engine*"]
